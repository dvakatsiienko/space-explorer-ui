# Query
query Launches($after: Int) {
    launches(after: $after) {
        cursor
        hasMore
        launches {
            ...LaunchFragment
        }
    }
}

query Launch($id: ID!) {
    launch(id: $id) {
        ...LaunchFragment
    }
}

# Fragment
fragment LaunchFragment on Launch {
    __typename
    id
    isBooked
    site
    rocket {
        id
        name
        type
    }
    mission {
        name
        missionPatch
    }
}
